import styled from "styled-components"
const Footer = () => {
    return (
        <FooterContainer>
            cc Minimal logic 
        </FooterContainer>
    )
}

export default Footer

const FooterContainer = styled.footer`
    text-align:center;
`