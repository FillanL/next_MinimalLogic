import Seo from "../components/Seo";
const Explore = (): React.ReactElement => {
  return (
    <div>
      <Seo title={``} description={`page for site`} content={`explore page`} />
      explore all blog post
    </div>
  );
};

export default Explore;
